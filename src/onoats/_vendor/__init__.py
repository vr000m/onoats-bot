# Thin subset vendored from koda's shared/ package. Each module carries a
# "vendored from koda shared/<name>.py" provenance comment at the top so the
# validation whitelist can distinguish a legitimate provenance reference from
# residual coupling. Only the DB-free, recorder-relevant helpers are kept.
